# RUN_STATUS — чесний стан проєкту

Оновлено: 2026-08-04 · версія 0.2.0

## Команди та результат (перевірено цього прогону)

| Команда | Результат |
|---------|-----------|
| `npm run typecheck` (`tsc -b`) | ✅ 0 помилок |
| `npm run lint` | ✅ 0 помилок, 1 попередження (react-refresh, нешкідливе) |
| `npm test` (Vitest) | ✅ **39/39** (8 файлів, у т.ч. 300 property-based сценаріїв) |
| `npm run build` / `build:pages` | ✅ збірка + PWA; base '/' і `/subpath/` перевірено |
| `scripts/check-pwa.mjs dist` | ✅ manifest/іконки/sw валідні |
| `npm run test:e2e` (Playwright) | ✅ **15/15** (desktop-chrome, mobile-chrome, mobile-safari) |
| HTTP-перевірка `dist` (vite preview) | ✅ index/manifest/sw/іконки/deep-route → 200 |
| Немає горизонтального скролу | ✅ 375, 430, 1440 |

## Реалізовано й перевірено цього циклу

- ✅ **Нульовий старт**: seed-суми = 0; приклади — лише placeholder; накопичення 0.
  Перевірено в браузері та E2E (поля порожні, «Ще немає історії витрат»).
- ✅ **Міграції** (Dexie v2, `appMigrations`): ідемпотентне обнулення недоторканих
  стартових сум; не чіпає відредаговані/використані записи. 5 тестів.
- ✅ **Рекомендації**: без історії НЕ вигадують суму (0, `hasHistory=false`);
  «Прийняти» лише за наявності закритих місяців; сума не стає планом автоматично.
- ✅ **Редактор категорій**: створення/редагування/дублювання/порядок ↑↓/
  вимкнення/архів/відновлення/безпечне видалення; розділи; пошук/фільтри/архівні.
  Перевірено в браузері (створення «Книги» → збережено в IndexedDB, min/desired=0).
- ✅ **Безпечне видалення**: використовувані категорії архівуються, історія й
  закриті місяці не ламаються.
- ✅ **PWA для GitHub Pages**: `VITE_BASE_PATH`, `base`, роутер `basename`,
  `build:pages`; manifest start_url/scope/id узгоджені з base.
- ✅ **PWA UX**: банер оновлення (контрольований), підказка встановлення
  (iOS-інструкція / Android-кнопка), `navigator.storage.persist()`.
- ✅ **Демо-ізоляція**: окремий демо-місяць (`demoMonthId`), підтвердження,
  заборона повторного завантаження, окреме видалення демо.
- ✅ **Інваріантні тести** фінансів (300 сценаріїв) + **insights** (числові
  висновки) з тестами й інтеграцією в «Підсумки».
- ✅ **CI/CD**: `.github/workflows/deploy-pages.yml`, `scripts/setup-github-pages.sh`,
  `scripts/check-pwa.mjs`.
- ✅ Збереження даних переживає reload (E2E) та навігацію.

## Реалізовано частково

- 🟡 **Форма витрати**: спосіб оплати/коментар ще не сховані в явний блок
  «Додатково» (є, але не згорнуті). Обрані/останні категорії та «повтор останньої»
  — у беклозі.
- 🟡 **Графіки**: є числові висновки + бари доходу/витрат і порівняння періодів;
  окремих графіків «накопичення по місяцях / динаміка категорій / резерви» поки
  немає (є агрегати в `insights`).
- 🟡 **Розділи**: перейменування підтримано (`renameSection`), окремий екран
  керування розділами (порядок/архів розділу) — у беклозі; категорію можна
  перенести в новий розділ через форму.
- 🟡 **Drag-and-drop** порядку — замість нього кнопки ↑/↓.

## Не реалізовано (свідомо, поза межами)

- ⬜ Акаунти/сервер/хмара/синхронізація/аналітика (за вимогою — локально).
- ⬜ ru/en локалі (шар готовий; лишаємо лише uk за вимогою).
- ⬜ PIN-код/автоблокування (є `hideAmounts`; архітектура сумісна).

## Публікація — виконано ✅

- **Публічний URL:** https://deusrat-lab.github.io/campaign-timeline-vtt/
- Репозиторій: https://github.com/deusrat-lab/campaign-timeline-vtt
- Гілки: `deploy/budget-pwa` (код), `gh-pages` (статика). Гілку `master`
  (застосунок campaign-timeline-vtt) НЕ чіпав — апки не змішані.
- Метод: **deploy-from-branch** (токен scope `repo` без `workflow`; Actions-workflow
  збережено як референс у `docs/ci/`).
- GitHub Pages: статус `built`. LIVE перевірено: усі ресурси 200, SW зареєстровано
  й керує сторінкою, офлайн-shell у precache, HashRouter deep-link рендериться,
  IndexedDB зберігає дані після reload, без 404/помилок у консолі.
- Офлайн-автотест (Playwright, production build): 2/2 (Chromium desktop+mobile).

## Потребує ручної перевірки на пристрої

- Реальні iPhone 15 Plus та OnePlus 13 недоступні в середовищі. Емуляцію (розміри,
  теми, відсутність H-скролу) перевірено; апаратне встановлення PWA, Dynamic
  Island, системну кнопку «назад», повернення з фону — **не перевірялося**.
- Реальний офлайн-перезапуск найкраще підтвердити вручну (DevTools → Offline →
  reload).

## Відомі обмеження

- Дані лише в IndexedDB цього браузера/PWA; очищення даних сайту стирає історію →
  потрібні експорти (є нагадування й `storage.persist`).
- 1 lint-попередження (react-refresh на `ui.tsx`) — не впливає на роботу.

## Наступний конкретний крок

Виконати `./scripts/setup-github-pages.sh <user> <repo>` + `git push` + увімкнути
Pages (GitHub Actions) і вписати публічний URL у README та цей файл.
