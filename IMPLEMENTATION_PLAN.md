# План реалізації

Легенда: ✅ зроблено · 🟡 частково · ⬜ заплановано.

## Етап 1 — аудит і запуск проєкту ✅
- ✅ Аудит порожньої директорії, ініціалізація git.
- ✅ React + TypeScript + Vite, залежності, dev-сервер.
- ✅ TypeScript (project references), ESLint, Vitest, PWA (vite-plugin-pwa).

## Етап 2 — доменна модель і база ✅
- ✅ Грошовий тип (цілі копійки) + тести.
- ✅ Моделі (categories, months, plans, transactions, transfers, reserves,
  savings, closures, recommendations, auditLog).
- ✅ Dexie-схема v1 з індексами, seed стартового шаблону, `ensureSeeded`.
- ✅ Zod-схеми для сутностей та резервної копії.

## Етап 3 — базовий інтерфейс ✅
- ✅ Layout, нижня навігація з FAB, теми (світла/темна/системна), safe-area.
- ✅ Головний екран, пусті стани, mobile-first адаптація.

## Етап 4 — створення місяця ✅
- ✅ Майстер 4 кроки: дохід частинами → рекомендації → резерв/накопичення →
  перевірка з попередженням про дефіцит і джерелом покриття.

## Етап 5 — операції ✅
- ✅ Швидке додавання витрати (bottom sheet, чипи категорій, прогноз залишку).
- ✅ Доходи, повернення, коригування (репозиторії), журнал з фільтрами.
- ✅ Тост зі «Скасувати» (soft-delete).

## Етап 6 — переноси та резерви ✅
- ✅ Аркуш переносу з безпечним порядком джерел, захист критичних категорій.
- ✅ Журнал переносів (залишки до/після), резерв місяця, накопичення.

## Етап 7 — закриття і рекомендації ✅
- ✅ Чек-лист перед закриттям, підсумок, аналіз категорій, рекомендації на
  наступний місяць, створення наступного місяця з пропозиціями.
- ✅ Повторне відкриття з підтвердженням та аудитом.

## Етап 8 — експорт, імпорт і PWA ✅
- ✅ Експорт/імпорт JSON з Zod, попередній перегляд, replace/merge, авто-бекап
  перед імпортом.
- ✅ manifest, service worker, офлайн-кеш, autoUpdate.

## Етап 9 — тестування та полірування 🟡
- ✅ Unit-тести (money, balances, recommendations) + integration (повний цикл).
- 🟡 Playwright E2E: сценарій написано; вимагає `npx playwright install`.
- 🟡 Компонентні тести React (RTL) — базовий каркас; розширюється.
- ✅ Перевірка адаптивності на mobile-viewport у браузері.

## Далі (беклог)
- ⬜ Редактор категорій в UI (створення/зміна власних категорій та розділів).
- ⬜ Графіки динаміки головних категорій (зараз — базові бари доходу/витрат).
- ⬜ Опційний PIN-код та авто-блокування.
- ⬜ ru/en локалі (шар готовий).
- ⬜ Серверна синхронізація (модель готова).
