node_modules
dist
dev-dist
*.local
.DS_Store
coverage
playwright-report
test-results
.vscode
dist-artifacts
.pages-serve
# Локальні артефакти release-валідації — можуть містити реальні фінансові дані
# користувача (fingerprint суми/ID з реального бекапу). Ніколи не комітити.
artifacts
